services:
  kafka:
    image: confluentinc/confluent-local:7.4.3
    hostname: kafka
    container_name: kafka
    ports:
      - "9092:9092" # for client connections
      - "9101:9101" # JMX
    restart: unless-stopped
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092'
      KAFKA_JMX_PORT: 9101
      KAFKA_JMX_HOSTNAME: localhost
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:9092'
      CLUSTER_ID: 'K0EA9p0yEe6MkAAAAkKsEg'

  kafka-init-topics:
    image: confluentinc/confluent-local:7.4.3
    container_name: kafka-init-topics
    depends_on:
      - kafka
    command: "bash -c \
                'kafka-topics --create --topic telemetry.sensors.v1 \
                             --partitions 1 --replication-factor 1 --if-not-exists \
                             --bootstrap-server kafka:29092 && \
                kafka-topics --create --topic telemetry.snapshots.v1 \
                             --partitions 1 --replication-factor 1 --if-not-exists \
                             --bootstrap-server kafka:29092 && \
                kafka-topics --create --topic telemetry.hubs.v1 \
                             --partitions 1 --replication-factor 1 --if-not-exists \
                             --bootstrap-server kafka:29092'"
    init: true

  analyzer-db:
    image: postgres:16.1
    container_name: postgres-analyzer-db
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_DB=analyzerdb
      - POSTGRES_USER=analyzer
      - POSTGRES_PASSWORD=analyzer

  commerce-cart-db:
    image: postgres:16.1
    container_name: postgres-commerce-cart-db
    ports:
      - "5435:5432"
    environment:
      - POSTGRES_DB=commercecartdb
      - POSTGRES_USER=cart
      - POSTGRES_PASSWORD=cart

  commerce-store-db:
    image: postgres:16.1
    container_name: postgres-commerce-store-db
    ports:
      - "5436:5432"
    environment:
      - POSTGRES_DB=commercestoredb
      - POSTGRES_USER=store
      - POSTGRES_PASSWORD=store

  commerce-warehouse-db:
    image: postgres:16.1
    container_name: postgres-commerce-warehouse-db
    ports:
      - "5437:5432"
    environment:
      - POSTGRES_DB=commercewarehousedb
      - POSTGRES_USER=warehouse
      - POSTGRES_PASSWORD=warehouse

  order-db:
    image: postgres:16.1
    container_name: postgres-order-db
    ports:
      - "5438:5432"
    environment:
      POSTGRES_DB: order
      POSTGRES_USER: order
      POSTGRES_PASSWORD: order

  payment-db:
    image: postgres:16.1
    container_name: postgres-payment-db
    ports:
      - "5439:5432"
    environment:
      POSTGRES_DB: payment
      POSTGRES_USER: payment
      POSTGRES_PASSWORD: payment

  delivery-db:
    image: postgres:16.1
    container_name: postgres-delivery-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: delivery
      POSTGRES_USER: delivery
      POSTGRES_PASSWORD: delivery